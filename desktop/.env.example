# Backend URL for production (leave empty in dev to use proxy)
VITE_BACKEND_URL=

# Tauri development host (auto-set by Tauri CLI usually)
TAURI_DEV_HOST=

# Tauri signing key password (required for signed builds)
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
